version: "2"
sql:
  - schema: "/sql/schema"
    queries: "/sql/queries"
    engine: "postgresql"
    database:
      uri: "postgres://postgres:root@localhost:5432/colibrary"
    gen:
      go:
        out: "internal/database"
